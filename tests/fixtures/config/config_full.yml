mongo_db_bundle:
    clients:
      test_client:
        hosts: localhost:8080
        username: foo
        password: bar

    connections:
      test_db:
        client_name: test_client
        database_name: testdb
