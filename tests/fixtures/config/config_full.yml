mongo_db_bundle:
    default_connection: default # No sense to specify it. This is it just for test coverage.
    host: localhost
    database: testdb
    port: 27017
    username: 'foo'
    password: 'bar'