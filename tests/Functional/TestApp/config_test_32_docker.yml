framework:
  secret: "Four can keep a secret, if three of them are dead."
  annotations:
    enabled: false

mongo_db_bundle:
  clients:
    test_client:
      uri: mongodb://root:rootPass@mongo
      options:
        authSource: admin

  connections:
    test_db:
      client_name: test_client
      database_name: testFunctionaldb
